name: super_text_editor_example
description: A comprehensive example app demonstrating super_text_editor features.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  super_editor: ^0.3.0-dev.35
  super_text_editor:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
