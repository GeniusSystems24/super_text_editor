name: super_text_editor
description: "A powerful, customizable rich text editor for Flutter with comprehensive formatting options, tables, lists, and HTML support. Similar to CKEditor 5."
version: 0.2.0
homepage: https://github.com/GeniusSystems24/super_text_editor
repository: https://github.com/GeniusSystems24/super_text_editor
issue_tracker: https://github.com/GeniusSystems24/super_text_editor/issues

environment:
  sdk: ^3.10.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
