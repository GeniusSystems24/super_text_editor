name: super_text_editor
description: "A powerful native Flutter document editor with support for rich text, tables, lists, and more. Built on a Document/Node architecture without WebView."
version: 1.0.0
homepage: https://github.com/GeniusSystems24/super_text_editor
repository: https://github.com/GeniusSystems24/super_text_editor
issue_tracker: https://github.com/GeniusSystems24/super_text_editor/issues

environment:
  sdk: ^3.10.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  super_editor: ^0.3.0-dev.35
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
